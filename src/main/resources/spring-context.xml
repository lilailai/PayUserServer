<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans  
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd  
           http://www.springframework.org/schema/aop   
           http://www.springframework.org/schema/aop/spring-aop-3.2.xsd  
           http://www.springframework.org/schema/tx  
           http://www.springframework.org/schema/tx/spring-tx-3.2.xsd  
           http://www.springframework.org/schema/context  
           http://www.springframework.org/schema/context/spring-context-3.2.xsd
           http://code.alibabatech.com/schema/dubbo  
           http://code.alibabatech.com/schema/dubbo/dubbo.xsd"
	default-autowire="byName" default-lazy-init="false">


	<bean id="configProperties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
		<property name="locations">
			<list>
				<value>File:/home/weblogic/etc/PayImobProject/config.properties
				</value>
			</list>
		</property>
	</bean>


	<!-- 属性参数加密码配置 -->
	<bean id="propertyConfig" class="com.imobpay.base.config.PropertyPlaceholderConfigurerExt">
		<property name="locations">
			<list>
				<value>File:/home/weblogic/etc/PayImobProject/config.properties
				</value>
			</list>
		</property>
	</bean>

	<!-- 对态加载注解 -->
	<context:component-scan annotation-config="true" base-package="com.imobpay.base.*" />
	<!-- 采用注释的方式配置bean -->
	<context:annotation-config />



	<import resource="spring-db.xml" />
	<import resource="spring-provider.xml" />
	<import resource="spring-tibco.xml" />



	<!-- 加密机 -->
	<!-- <bean id="sendEncryptConfig1" class="com.imobpay.base.config.SendEncryptConfig"> -->
	<!-- <property name="url" value="${Encrypt.url}" /> -->
	<!-- <property name="port" value="${Encrypt.port}" /> -->
	<!-- <property name="timeOut" value="${Encrypt.timeOut}" /> -->
	<!-- <property name="encode" value="${Encrypt.encode}" /> -->
	<!-- <property name="recLen" value="${Encrypt.recLen}" /> -->
	<!-- <property name="lenType" value="${Encrypt.lenType}" /> -->
	<!-- <property name="result" value="${Encrypt.result}" /> -->
	<!-- <property name="sendlen" value="${Encrypt.sendlen}" /> -->
	<!-- <property name="sendlenFill" value="${Encrypt.sendlenFill}" /> -->
	<!-- <property name="addHeadData" value="${Encrypt.addHeadData}" /> -->
	<!-- <property name="addTrailData" value="${Encrypt.addTrailData}" /> -->
	<!-- <property name="addLen" value="${Encrypt.addLen}" /> -->
	<!-- <property name="addDataTail" value="${Encrypt.addDataTail}" /> -->
	<!-- </bean> -->



</beans>